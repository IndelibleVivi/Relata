# System Card：<系统名称>

**Card ID：** SC-000  
**Status：** draft | contributor-reviewed | accepted-for-census | superseded  
**可见范围：** public | restricted | private summary  
**Contribution ID：**  
**贡献者署名：**  
**贡献者描述这套系统的 authority：**  
**Exact version 或 observation date：**

> 这不是产品评分表。先用系统自己的 vocabulary 讲清真实结构，再讨论它怎样映射到 Relata 的拟议 boundary。

## 1. System identity 与 scope

- 主要用途、relationship / agent role：
- local、hosted 或 mixed：
- 包含哪些 components：
- 明确不包含哪些 components：
- 当前成熟度：
- 可用 evidence：public source | contributor description | reproduced probe | other

## 2. System-native map

按真实 components 与 flows 作图或描述，不强行套成 event → retrieval → context pipeline。Manual curation、model-mediated behavior、latent state、platform service 与 opaque stage 都可以原样保留。

```text
<用这套系统自己的 topology 与 vocabulary 替换>
```

## 3. Continuity-bearing material

系统长期 retained、reconstructed、inferred、rehearsed 或 made available 的是什么？可以是 raw history、summary、embedding、graph、files、prompt、model state、ritual、decision、identity material、human curation，也可以没有可 inspect 的离散对象。

## 4. Change 与 repair

材料或行为怎样改变？描述 correction、supersession、coexistence、uncertainty、expiry、deletion、reauthorization、manual editing、consolidation 或 unsupported route。哪里可能 silent failure？

## 5. Activation 与 use

过去的关系材料怎样影响 current response 或 action？描述 trigger、search、prompt assembly、context、model mediation、tools、reminders 或 opaque behavior，不要发明系统没有暴露的 stages。

## 6. Source、authority 与 perspective

系统是否以及怎样区分 human statement、companion inference、third-party claim、recorder interpretation、explicit acceptance、dispute 与 uncertainty？

## 7. Time

系统中哪些 time 存在或重要：event time、record time、current time、effective interval、sequence only、model session，或完全没有 explicit representation？怎样保留 historical material 又不把它当 current guidance？

## 8. Surfaces、models 与 identity

private chat、group chat、CLI、voice、roleplay、projects、devices、models、providers、accounts 与 instances 之间，什么共享、什么隔离？migration 时什么改变？

## 9. Prospective behavior

系统能否保留 future intention、trigger、expiry、wake action 或 reminder？如果不适用，写明原因。

## 10. Contributor control 与 user sovereignty

谁能 inspect、explain、amend、revoke、delete、export、rebuild 或 migrate continuity-bearing material？哪些部分在技术或制度上仍然 opaque？

## 11. Known successes 与 failures

写具体 behavior、evidence class 与 version。分开 product claim、contributor experience 与 reproduced observation。

## 12. Candidate Relata boundaries 下的 observability

使用 `systems/README.md` 定义的 `N`、`E`、`O`、`U`、`?`、`—`。

| Candidate observation | Class | Evidence | Distortion note |
|---|---:|---|---|
| ordered history exposure |  |  |  |
| distinct ingest / write output |  |  |  |
| retrieval / activation material |  |  |  |
| rendered model context |  |  |  |
| final response or action |  |  |  |
| correction / revocation route |  |  |  |
| inspection / explanation route |  |  |  |
| time advance / future wake |  |  |  |

## 13. Candidate Relata probes

哪一个 incident、distinction 或 pilot 最适合用这套系统 pressure-test？哪条 output lane 能保留系统本身，而不是测 adapter reconstruction？

## 14. Confidentiality 与 publication limits

Relata 不得公开、推断或索取什么？填好的 restricted card 与 identifying consent record 必须留在 public repo 之外。

## 15. Contributor review

- 这份描述足以支持 declared use：是 | 否
- public-safe fields 已批准：是 | 否 | 不公开
- 保留的 disagreement / unknown：
- review date：
- reviewer / contributor confirmation route：
